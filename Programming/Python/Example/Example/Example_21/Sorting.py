import os
import sys


# 값을 정렬한다
def sortValues_Selection(a_oListValues):
	pass


# 값을 정렬한다
def sortValues_Heap(a_oListValues):
	pass


# 값을 정렬한다
def sortValues_Heap_Internal(a_oListValues, a_nIdx_Start, a_nNumValues):
	pass


# 값을 정렬한다
def sortValues_Quick(a_oListValues):
	sortValues_Quick_Internal(a_oListValues, 0, len(a_oListValues) - 1)


# 값을 정렬한다
def sortValues_Quick_Internal(a_oListValues, a_nLeft, a_nRight):
	pass
