import os
import sys


# 값을 정렬한다
def sortValues_Bubble(a_oListValues):
	pass


# 값을 정렬한다
def sortValues_Insertion(a_oListValues):
	pass


# 값을 정렬한다
def sortValues_Merge(a_oListValues):
	oListValues_Temp = [0] * len(a_oListValues)
	sortValues_Merge_Internal(a_oListValues, 0, len(a_oListValues) - 1, oListValues_Temp)


# 값을 정렬한다
def sortValues_Merge_Internal(a_oListValues, a_nLeft, a_nRight, a_oOutListValues_Temp):
	pass
